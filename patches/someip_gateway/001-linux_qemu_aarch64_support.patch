diff --git a/quality/integration_testing/plugins/linux_qemu/__init__.py b/quality/integration_testing/plugins/linux_qemu/__init__.py
index 128b280c..ccc83d06 100644
--- a/quality/integration_testing/plugins/linux_qemu/__init__.py
+++ b/quality/integration_testing/plugins/linux_qemu/__init__.py
@@ -100,6 +100,18 @@ def pytest_addoption(parser):
         required=True,
         help="Path to a QEMU disk image (qcow2).",
     )
+    parser.addoption(
+        "--qemu-kernel",
+        action="store",
+        default=None,
+        help="Path to a Linux kernel image for booting raw aarch64 disk images.",
+    )
+    parser.addoption(
+        "--qemu-kernel-cmdline",
+        action="store",
+        default="root=/dev/vda1 sdk_enable lisa_syscall_whitelist=2026 rw sharedmem.enable_sharedmem=0 init=/usr/bin/ebclfsa-cflinit",
+        help="Kernel command line used when --qemu-kernel is provided.",
+    )
     parser.addoption(
         "--qemu-seed-iso",
         action="store",
@@ -126,6 +138,10 @@ def config(request):
     qemu_seed_iso = request.config.getoption("qemu_seed_iso")
     if qemu_seed_iso:
         qemu_seed_iso = os.path.abspath(qemu_seed_iso)
+    qemu_kernel = request.config.getoption("qemu_kernel")
+    if qemu_kernel:
+        qemu_kernel = os.path.abspath(qemu_kernel)
+    qemu_kernel_cmdline = request.config.getoption("qemu_kernel_cmdline")
 
     return Bunch(
         qemu_config=load_configuration(
@@ -133,6 +149,8 @@ def config(request):
         ),
         qemu_image=qemu_image,
         qemu_seed_iso=qemu_seed_iso,
+        qemu_kernel=qemu_kernel,
+        qemu_kernel_cmdline=qemu_kernel_cmdline,
     )
 
 
@@ -156,7 +174,7 @@ def target_init(config, request, dlt):
                 "-b",
                 base_image,
                 "-F",
-                "qcow2",
+                "raw" if base_image.endswith(".wic") else "qcow2",
                 overlay_path,
             ],
             check=True,
@@ -171,6 +189,8 @@ def target_init(config, request, dlt):
             network_adapters=[adapter.name for adapter in config.qemu_config.networks],
             port_forwarding=config.qemu_config.port_forwarding,
             seed_iso=config.qemu_seed_iso,
+            path_to_qemu_kernel=config.qemu_kernel,
+            qemu_kernel_cmdline=config.qemu_kernel_cmdline,
         )
 
         with process:
diff --git a/quality/integration_testing/plugins/linux_qemu/qemu.py b/quality/integration_testing/plugins/linux_qemu/qemu.py
index cb4b9e4e..7a6ee374 100644
--- a/quality/integration_testing/plugins/linux_qemu/qemu.py
+++ b/quality/integration_testing/plugins/linux_qemu/qemu.py
@@ -21,6 +21,7 @@ import logging
 import os
 import subprocess
 import sys
+import platform
 from subprocess import TimeoutExpired
 
 logger = logging.getLogger(__name__)
@@ -39,21 +40,39 @@ class DiskBootQemu:
         ram="1G",
         cores="2",
         seed_iso=None,
-        cpu="Cascadelake-Server-v5",
+        cpu="cortex-a53",
+        path_to_kernel=None,
+        kernel_cmdline="root=/dev/vda1 sdk_enable lisa_syscall_whitelist=2026 rw sharedmem.enable_sharedmem=0 init=/usr/bin/ebclfsa-cflinit",
         network_adapters=None,
         port_forwarding=None,
     ):
-        self._qemu_path = "/usr/bin/qemu-system-x86_64"
+        self._qemu_path = "/usr/bin/qemu-system-aarch64"
         self._path_to_image = path_to_image
+        self._path_to_kernel = path_to_kernel
         self._ram = ram
         self._cores = cores
         self._seed_iso = seed_iso
         self._cpu = cpu
+        self._kernel_cmdline = kernel_cmdline
+
+        image_lower = path_to_image.lower()
+        if image_lower.endswith(".wic") or image_lower.endswith(".img"):
+            self._disk_format = "raw"
+        else:
+            self._disk_format = "qcow2"
+
+        # EBcLfSA images require an explicit kernel when booting the raw .wic disk.
+        self._use_kernel_boot = path_to_kernel is not None
         self._network_adapters = network_adapters or []
         self._port_forwarding = port_forwarding or []
 
         self._check_qemu_is_installed()
         self._find_available_kvm_support()
+        if platform.machine() != "aarch64":
+            logger.warning(
+                "Cross-architecture emulation; KVM unavailable, using TCG accelerator."
+            )
+            self._accelerator = "tcg"
         self._check_kvm_readable_when_necessary()
 
         self._subprocess = None
@@ -126,10 +145,32 @@ class DiskBootQemu:
             self._cpu,
             "-m",
             self._ram,
-            "-drive",
-            f"file={image_path},format=qcow2,if=virtio",
         ]
 
+        if self._use_kernel_boot:
+            kernel_path = os.path.abspath(self._path_to_kernel)
+            cmd.extend(
+                [
+                    "-machine",
+                    "virt,virtualization=true,gic-version=3",
+                    "-kernel",
+                    kernel_path,
+                    "-append",
+                    self._kernel_cmdline,
+                    "-device",
+                    "virtio-blk-device,drive=vd0",
+                    "-drive",
+                    f"if=none,format={self._disk_format},file={image_path},id=vd0",
+                ]
+            )
+        else:
+            cmd.extend(
+                [
+                    "-drive",
+                    f"file={image_path},format={self._disk_format},if=virtio",
+                ]
+            )
+
         cmd += ["-enable-kvm"] if self._accelerator == "kvm" else ["-accel", "tcg"]
 
         if self._seed_iso:
@@ -148,8 +189,12 @@ class DiskBootQemu:
         cmd.extend(
             [
                 "-nographic",
+                "-chardev",
+                "stdio,id=char0,signal=on,mux=on",
+                "-mon",
+                "chardev=char0,mode=readline",
                 "-serial",
-                "mon:stdio",
+                "chardev:char0",
                 "-object",
                 "rng-random,filename=/dev/urandom,id=rng0",
                 "-device",
@@ -171,20 +216,21 @@ class DiskBootQemu:
                         "-netdev",
                         f"tap,id=t{idx},ifname={adapter},script=no,downscript=no",
                         "-device",
-                        f"virtio-net-pci,netdev=t{idx},id=nic{idx},guest_csum=off",
+                        f"virtio-net-device,netdev=t{idx},id=nic{idx},guest_csum=off",
                     ]
                 )
         return result
 
     def _port_forwarding_args(self):
         result = []
+        netdev = "virtio-net-device"  # if self._use_kernel_boot else "virtio-net-pci"
         for idx, forwarding in enumerate(self._port_forwarding, start=1):
             result.extend(
                 [
                     "-netdev",
                     f"user,id=net{idx},hostfwd=tcp::{forwarding.host_port}-:{forwarding.guest_port}",
                     "-device",
-                    f"virtio-net-pci,netdev=net{idx}",
+                    f"{netdev},netdev=net{idx}",
                 ]
             )
         return result
diff --git a/quality/integration_testing/plugins/linux_qemu/qemu_process.py b/quality/integration_testing/plugins/linux_qemu/qemu_process.py
index 20fcc0bf..9ddebcc8 100644
--- a/quality/integration_testing/plugins/linux_qemu/qemu_process.py
+++ b/quality/integration_testing/plugins/linux_qemu/qemu_process.py
@@ -32,6 +32,8 @@ class LinuxQemuProcess:
         network_adapters=None,
         port_forwarding=None,
         seed_iso=None,
+        path_to_qemu_kernel=None,
+        qemu_kernel_cmdline=None,
     ):
         self._path_to_qemu_image = path_to_qemu_image
         self._available_ram = available_ram
@@ -43,6 +45,8 @@ class LinuxQemuProcess:
             self._available_ram,
             self._available_cores,
             seed_iso=seed_iso,
+            path_to_kernel=path_to_qemu_kernel,
+            kernel_cmdline=qemu_kernel_cmdline,
             network_adapters=self._network_adapters,
             port_forwarding=self._port_forwarding,
         )
